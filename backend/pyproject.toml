[project]
name = "chatgpt-web-service"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "httpx>=0.27.0",
  "python-dotenv>=1.0.1"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
